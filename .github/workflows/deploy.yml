name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: npx vercel --prod --yes --token ${{ secrets.VERCEL_TOKEN }} --scope smindon-5575s-projects --name viralarc-creator
